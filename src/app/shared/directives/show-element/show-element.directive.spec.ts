import { ShowElementDirective } from './show-element.directive';

describe('ShowElementDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowElementDirective();
    expect(directive).toBeTruthy();
  });
});
